import { SkeletonLoaderPipe } from './skeleton-loader.pipe';

describe('SkeletonLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new SkeletonLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
